import React from "react";

const DeleteKey = ({ onClick }) => {
  return <button onClick={onClick} className="bg-secondary rounded-full text-center col-start-4 col-end-5">X</button>;
};

export default DeleteKey;
